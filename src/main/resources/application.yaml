Spring:
  jpa:
    hibernate:
      ddl-auto: update
      jdbc:
        time_zone: ICT
        generate-ddl: true
        show-sql: true
  servlet:
    multipart:
      enabled: true
      max-file-size: 5MB
      max-request-size: 5MB
  datasource:
    url: jdbc:postgresql://localhost:5432/dashboard
    username: postgres
    password: Porpopor
  security:
    user:
      name: porpopor
      password: porpopor

app:
  token:
    secret: m#topSecret
    issuer: BackendService